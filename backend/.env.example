# ---- DeepGuard AI backend environment ----
# Copy to .env and adjust. Never commit the real .env.

SECRET_KEY=replace-with-a-long-random-string
JWT_SECRET_KEY=replace-with-a-different-long-random-string
JWT_EXPIRES_HOURS=2
FLASK_DEBUG=false

# sqlite (default) or mysql:
#   DATABASE_URL=mysql+pymysql://user:pass@localhost/deepguard
DATABASE_URL=sqlite:///deepfake.db

MAX_UPLOAD_MB=50

# Set true only after you place trained model weights in backend/models/weights
MODEL_ENABLED=false
MODEL_PATH=./models/weights
HF_MODEL_NAME=roberta-base-openai-detector

CORS_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW_SECONDS=60

# Bootstrap admin account (created on first start)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@deepguard.local
ADMIN_PASSWORD=Admin@12345
