# Optional analysis/kaggle dependencies for the heuristic engine + Kaggle data pipeline.
# pip install -r requirements-ai.txt
numpy==1.26.4
opencv-python==4.10.0.84
librosa==0.10.2
mediapipe
kaggle==2.2.4
# Local offline AI models (image ViT + RoBERTa text detector) - optional.
# First scan auto-downloads weights to the Hugging Face cache (~500 MB).
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0
timm>=0.9.0
soundfile>=0.12.0
