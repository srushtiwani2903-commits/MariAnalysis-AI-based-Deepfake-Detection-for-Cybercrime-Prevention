# DeepGuard AI frontend environment
# Copy to .env (Create React App reads .env automatically)

# Backend API base URL (no trailing /api - it is appended automatically)
REACT_APP_API_URL=http://localhost:5000/api
